
<!DOCTYPE html>
<html lang="en">
<head>
  <title>Your resolution</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial scale=1.0">

    <!-- <link href='http://fonts.googleapis.com/css?family=Open+Sans:400,700|Bitter:400,700|Nothing+You+Could+Do'
    media='screen'rel='stylesheet' type='text/css'>
    <link rel="stylesheet" type="text/css" href="css/normal.css"/>
   -->

  <link rel="stylesheet" type="text/css" href="css/bootstrap.css"/>
  <link rel="stylesheet" type="text/css" href="css/main.css"/>
</head>

<%=link_to "New User", new_user_url%>
<ul><% @users.each do |user| %>
<li><%= user.first_name  %>
<%= user.last_name %><br>
<%= user.email %><br>
<%= link_to image_tag(user.pic_url, width:200), user_url(user.id) %></li>
  <% if user.goals.present?%>
    <%= user.goals.first.name%>
    <% end %>
<% end %>
</ul>

